{
  "name": "u-stock-root",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "test:frontend": "npm --prefix frontend run test:frontend",
    "test:frontend:ui": "npm --prefix frontend run test:ui",
    "test:frontend:cov": "npm --prefix frontend run test:frontend -- --coverage",

    "test:backend": "cross-env ENV=test PYTHONPATH=backend pytest -q --no-cov backend/api",
    "test:backend:unit": "cross-env ENV=test PYTHONPATH=backend pytest -q --no-cov backend/api -m \"not slow\"",
    "test:backend:cov": "cross-env ENV=test PYTHONPATH=backend pytest -q backend/api --cov=backend/api --cov-report=term-missing",

    "test:bots": "cross-env ENV=test PYTHONPATH=u-stock-bots pytest -q --no-cov u-stock-bots",
    "test:bots:unit": "cross-env ENV=test PYTHONPATH=u-stock-bots pytest -q --no-cov u-stock-bots -m \"not slow\"",

    "test:quick": "npm run test:frontend && npm run test:backend:unit && npm run test:bots:unit",
    "test:all": "npm run test:frontend && npm run test:backend && npm run test:bots"
  },
  "devDependencies": {
    "cross-env": "^10.1.0"
  }
}
